let a = 2;
let b = 1 + a;


// ifs , else ifs , elses
if( true ) {
    let c = 0;
    c + a;
} elseif(false) {
    // k;
    a;
} else {
    b;
}

// for loops
for(let i = 0; i < 5; i = i + 1) {
    i + 1;   
    a;
    b;
    let c = 1 * 2;
}

// while loops
while(false) {
    let k = 0;
    let a = 2 + k;
}

// func declaration
func hello(a = 15, b , c = 'ur mom', d = 6) {
    let a = d + c;
    let l = 'hello';
    return a + l;
}

hello(a,b,1, 2 * 4);

// arrays
[a,b, 123];
     

// hash map
{
    a: 1,
    b: a,
    c: b,
    d: a + b
};

let n = n;
a();
hello();